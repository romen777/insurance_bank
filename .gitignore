# 로컬 자격증명 — Supabase service 키가 들어간다. 절대 커밋하지 않는다.
.env
.env.*

__pycache__/
*.pyc

# 검색 색인 — chunks.jsonl 에서 언제든 다시 만든다
index/

# 나눠 놓은 PDF — source/ 의 원본에서 잘라낸 것이라 저장소에 둘 이유가 없다
# (용량 제한이 있는 서비스에 올릴 때만 로컬에서 쓴다)
심사지침_*부_*.pdf

# 동기화 로그
data/sync_*.log
